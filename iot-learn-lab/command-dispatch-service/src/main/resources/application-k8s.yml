# 激活方式：SPRING_PROFILES_ACTIVE=k8s
# 无 DB / Redis / Nacos；仅启用探针端点
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      probes:
        enabled: true